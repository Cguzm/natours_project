
header.header
    nav.nav.nav--tours
        a.nav__el(href='/') All tours
    .header__logo
        img(src='/img/logo-white.png' alt='Natours logo')    
    nav.nav.nav--user
        if user
            a.nav__el(href='/me')
                span Account 
            a.nav__el.nav__el--logout Log out
            a.nav__el
                //- Upon user signup use the default img path otherwise use update img path  
                - const urlPath = user.photo.startsWith('user') || user.photo === 'default.jpg' ? `/img/users/${user.photo}` : `${user.photo}`;
                img.nav__user-img(src=`${urlPath}` alt=`Photo of ${user.name}`)
                span= user.name.split(' ')[0]  
        else 
            a.nav__el(href='/login') Log in
            a.nav__el.nav__el--cta(href='/signup') Sign up  